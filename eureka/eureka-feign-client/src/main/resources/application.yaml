spring:
  application:
    name: eureka-feign-client  # 配置程序名
server:
  port: 8765 # 设置此服务的端口
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/  # 向服务注册中心注册此服务。
feign:
  hystrix:
    enabled: true
